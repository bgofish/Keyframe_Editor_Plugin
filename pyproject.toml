[project]
name = "Keyframe_Editor"
version = "0.1.2"
description = "Edit timeline keyframes in a spreadsheet-style panel"
authors = [{name = "Brian Davis (bb6)"}]
dependencies = []

[tool.lichtfeld]
hot_reload = true
plugin_api = ">=1,<2"
lichtfeld_version = ">=0.5.0"
required_features = []
