[project]
name = "Keyframe_Editor"
version = "0.1.6"
description = "Edit timeline keyframes in a Table-style panel "
authors = "Brian Davis (bb6)"
dependencies = []

[tool.lichtfeld]
hot_reload = true
plugin_api = ">=1,<2"
lichtfeld_version = ">=0.5.0"
required_features = []
